import { Fab } from '@material-ui/core';
import Button from '../@material-extend/MButton';

export default function TurnstileComponent() {
  return (
    <Fab variant="extended">
      <Button />
    </Fab>
  );
}
